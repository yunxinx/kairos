# Rust 构建产物
/target

# SQLite 动态状态（令牌余额、用量、日志）
*.db
*.db-journal
*.db-wal
*.db-shm

# 本地运行目录 .kairos/（配置文件与数据库，含上游密钥与下游令牌，禁止入库）
/.kairos/

# 运行时日志
*.log

# insta 待接受快照：只应存在于本地迭代期间，由 `cargo insta test --accept` 消化
*.snap.new

# 编辑器与工具
.vscode/
.idea/
*.swp

# 操作系统
.DS_Store
Thumbs.db

# Web UI
/webui/node_modules/
/webui/dist/
/webui/playwright-report/
/webui/test-results/
/webui/.tmp/
